export interface MappedResponse {
  purchase_id?: string;
  product_id: string;
  id: string;
  name: string;
  price: number;
  quantity: number;
  remaining_quantity: number;
  unit: string;
  hpp: number;
}